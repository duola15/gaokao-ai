# Netlify 部署配置 — Next.js 14+ App Router
[build]
  command = "npm run build"
  publish = ".next"

# Netlify Next.js 运行时插件（处理 SSR / API Routes / ISR）
[[plugins]]
  package = "@netlify/plugin-nextjs"

# 开发环境变量（生产环境在 Netlify 后台设置）
[build.environment]
  NEXT_TURBO = "0"
